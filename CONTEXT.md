# 旅行规划上下文

记录旅行规划的稳定术语和已确认的业务规则；使用方式与当前边界见 README，后续学习事项见学习计划。

## Language

**旅行需求**：用户提交的目的地、日期、偏好及其他约束的集合。

**一次规划**：基于一份旅行需求独立生成行程的执行过程。
_Avoid_: 会话（第一版不跨请求延续对话）

**行程**：一次规划正常完成后向用户交付的旅行安排。
_Avoid_: 将占位数据称为成功行程

**备用行程**：旧实现遇到异常后用占位景点等程序构造的数据形成的响应，当前版本不再交付此类结果。

**天气未知**：旅行日期没有对应的有效查询预报信息的状态。

### 路线规划术语

**生成完成**：一次规划产出了满足最低交付条件且已保存的行程，不代表所有安排都无需调整。

**行程问题**：行程中已知的安排冲突或尚未确认的信息，可在同一份行程中同时存在。

**需调整**：行程存在已识别的安排冲突、预算超限或部分天数缺少安排的提示。

**有待确认事项**：行程存在尚未核实的信息的提示。

**天气查询失败**：未能正常取得天气查询结果的情况，与正常查询后预报未覆盖旅行日期不同。

**已核实景点**：对应地图查询返回的具体地点且具有可用坐标的景点，不表示开放时间、价格或预约条件已经确认。
_Avoid_: 将模型推荐但未查到具体地点的内容称为已核实景点

**未能安排的必去地点**：用户指定必去但查不到或无法唯一定位、因而未进入正式路线的地点需求。

**行程活动**：每日安排中具有明确先后顺序的一项游览、用餐或自由活动，上午、下午等时段是其标签。

**路段**：每日路线中两个地点之间的一次移动安排，包含起终点、交通方式、预计耗时及查询或估算依据。

**区域参考点**：从地图查询结果中选取、用于估算区域相关路段的具体位置，不代表用户已选定的酒店或餐厅。

**自由活动**：预留给用户自行安排的一段时间，未指定地点时默认在上一活动附近进行，不产生新的路线地点。

**每日时间预算**：用于判断一天安排是否过满的时间上限，与游览、用餐、交通、自由活动及缓冲的合计时长比较。

**规划条件**：从旅行需求中统一整理的必去地点、预算口径、交通方式、偏好及提醒事项，保留用户原文供核对。

## Relationships

- 每份通过输入校验且在空闲时被接收的**旅行需求**发起一次新的**规划**，不同规划不共享对话历史；被拒绝的请求不算已开始的规划。
- 第一版同时只允许一个活动的**规划**；已有规划被接收或正在执行时拒绝新提交，不排队，但仍可查看当前状态和历史记录。
- **旅行需求**的天数按包含首尾日期的日期范围计算；旧客户端显式提交不一致的天数时拒绝请求，不启动外部调用。
- 输入校验被拒绝的请求不计入规划执行失败率，也不产生 Agent 或工具执行记录；在观测页面单独展示拒绝原因是后续需求。
- 浏览器关闭、刷新或断开连接不取消已开始的**规划**；后端继续执行，最终结果可在观测页面查看。后端进程退出导致未完成的规划记为执行中断。
- 重启后保留中断记录，不自动恢复或重新执行；再次规划由用户从首页重新提交旅行需求。
- 一次**生成完成**的规划交付一份满足最低条件并已保存的**行程**；任务状态不代表行程没有问题。
- 交付的**行程**起止日期必须与**旅行需求**一致，每日日期完整、连续且不重复，天数与需求一致；结构合法但不满足这些约束的结果仍判定为规划失败。
- 正式路线只使用具有地图查询依据与有效坐标的景点；仅有模型回答不能证明地点事实。
- 地点查询为空属于信息不足，搜集角色可在剩余额度内补充查询；完全没有已核实景点时规划失败。
- 天气查询成功且有有效数据，但预报未覆盖部分或全部旅行日期时，允许继续规划；对应日期标为**天气未知**，不补写天气或温度，不挪用其他日期的预报。
- 观测记录保存失败不改变**规划**的业务结果，但必须提示记录不完整。

## Example dialogue

> **开发者：** 天气查询失败，行程一定失败吗？
> **使用者：** 不一定。保留失败记录，提示未依据天气调整；满足最低条件仍可交付，但不能编造天气。

## Flagged ambiguities

- **生成完成**与**行程问题**分开表达，不引入“部分成功”的规划结果分类；有行程问题不等于生成失败。最低交付条件见[优化需求](docs/trip-planning-requirements.md#最低交付条件与结果提示已确认)。当前最小版本与尚未落实的目标以 README 和需求文档中的范围说明为准。
- **天气查询失败**不单独导致整份规划失败，行程须提示未根据天气调整；不得将查询失败表述为正常查询后的预报日期未覆盖。
- 正式路线只包含**已核实景点**；名称与位置取自查询结果，模型负责选择、排序和推荐理由。
- “必去景点全部保留”指不丢失用户需求及其处理结果；无法核实的地点保留为**未能安排的必去地点**并说明原因，其他已核实景点继续规划，不要求用户先澄清才生成。
- 每个游玩日包含一份有序的**行程活动**列表，游览、用餐和自由活动穿插排列；路线规划与页面展示遵循同一顺序。
- **行程活动**与**路段**分别表达活动内容和地点间的移动，页面按路线顺序穿插展示；从住宿基点出发与返回的移动也作为路段记录。
- 住宿区域和用餐区域通过明确标注的**区域参考点**参与路线计算；实际酒店或餐厅位置可能带来额外移动，不能将参考点交通耗时视为到具体商家的准确耗时。
- 无指定地点的**自由活动**之后，交通仍从上一已定位地点计算，须说明未包含用户自行离开该区域的路程。
- 用明确的**每日时间预算**检查安排是否过满；缺失耗时不按零计算，信息不足时不能断言整天安排可行。
- 一次规划共用一份统一整理的**规划条件**，用于地点搜集、行程安排、修改与规则检查；仅支持提醒的要求不能被表述为已落实到路线。
- 额外要求是一段完整备注，开放意图由模型理解；禁止通过关键词、正则或硬编码规则推断用户意图。原文持续保留，确定性数据检查不等于对意图理解正确性的验证。
- 模型识别出额外备注与用户明确填写的表单项冲突时，以表单项为准，继续规划并说明冲突；备注可以补充要求或调整默认值，不直接改写已选城市、日期等明确输入。
- 每项**行程问题**具有明确的类型、影响范围和说明，可关联整份行程、某一天、活动或路段；问题汇总、调整和页面定位使用同一份问题记录。
- 用户指定的住处无法定位时，允许保留该住处并交付地点间的路线，往返住处的交通标为未知；不擅自替换住处，也不编造位置。
- HTTP 成功、工具成功、生成完成与行程合理是不同结论，不相互替代。
- 带提示交付基于已有真实结果，不等于旧版编造占位备用行程。
- 天气预报未覆盖与天气查询失败分别提示，均不能补造天气。
