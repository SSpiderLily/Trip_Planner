# 旅行规划上下文

记录旅行规划的稳定术语和已确认的业务规则；目标规则与当前代码差异由临时实施计划跟踪。

## Language

**旅行需求**：用户提交的目的地、日期、偏好及其他约束的集合。

**一次规划**：基于一份旅行需求独立生成行程的执行过程。
_Avoid_: 会话（第一版不跨请求延续对话）

**行程**：一次规划正常完成后向用户交付的旅行安排。
_Avoid_: 将占位数据称为成功行程

**备用行程**：旧实现遇到异常后用占位景点等程序构造的数据形成的响应，目标版本不再交付此类结果。

**天气未知**：旅行日期没有对应的有效查询预报信息的状态。

## Relationships

- 每份通过输入校验且在空闲时被接收的**旅行需求**发起一次新的**规划**，不同规划不共享对话历史；被拒绝的请求不算已开始的规划。
- 第一版同时只允许一个活动的**规划**；已有规划被接收或正在执行时拒绝新提交，不排队，但仍可查看当前状态和历史记录。
- **旅行需求**的天数必须等于包含首尾日期的日期范围长度；不一致时直接拒绝请求，不自动修正，也不启动规划或外部调用。
- 输入校验被拒绝的请求仍可在观测页面查看原因，单独标为请求被拒绝，不计入规划执行失败率，也不产生 Agent 或工具执行记录。
- 浏览器关闭、刷新或断开连接不取消已开始的**规划**；后端继续执行，最终结果可在观测页面查看。后端进程退出导致未完成的规划记为执行中断。
- 重启后保留中断记录，不自动恢复或重新执行；再次规划由用户从首页重新提交旅行需求。
- 一次成功的**规划**交付一份**行程**；必要步骤失败时不交付行程，向用户返回明确错误。
- 交付的**行程**起止日期必须与**旅行需求**一致，每日日期完整、连续且不重复，天数与需求一致；结构合法但不满足这些约束的结果仍判定为规划失败。
- 景点、天气和酒店步骤成功的必要条件是对应查询工具实际执行成功；仅有模型回答不满足该条件。
- 必要查询正常返回空结果时，属于信息不足而非调用错误；第一版终止规划，不自动调整查询条件。
- 天气查询成功且有有效数据，但预报未覆盖部分或全部旅行日期时，允许继续规划；对应日期标为**天气未知**，不补写天气或温度，不挪用其他日期的预报。
- 观测记录保存失败不改变**规划**的业务结果，但必须提示记录不完整。

## Example dialogue

> **开发者：** 酒店查询失败，能用占位酒店返回行程吗？
> **使用者：** 不能，第一版应明确报告规划失败；但观测保存失败时，规划可以继续。

## Flagged ambiguities

- “成功”不能仅由 HTTP 200 或模型返回文本判定；景点、天气和酒店必须有对应查询实际成功的证据，此条件不代表结果一定足够或最终行程合理。
- “降级完成”是旧备用行程行为的描述，目标第一版不支持必要业务步骤失败后的部分交付。
- **天气未知**是日期覆盖不足的明确例外，不等于工具失败、空查询结果或备用行程；不放宽其他必要步骤的失败规则。
